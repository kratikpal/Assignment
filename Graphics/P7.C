#include <stdio.h>
// #include <graphics.h>
#include <conio.h>
#include <math.h>

int main()
{
    // int gd = DETECT, gm;
    // initgraph(&gd, &gm, "C:\\TURBOC3\\BGI");
    float a = sin(100 * 3.14 / 180);
    printf("%f", a);
    // closegraph();
    return 0;
}